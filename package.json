{
  "name": "dappsfactory-web",
  "type": "module",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "redirects": "node scripts/redirects.mjs",
    "ftp": "node scripts/ftp.mjs",
    "deploy:ftp": "npm run redirects && astro build && node scripts/ftp.mjs"
  },
  "dependencies": {
    "astro": "^7.3.3",
    "sharp": "^0.35.4"
  }
}
