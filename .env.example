# Endpoint que recibe el formulario de /contacto.
# El sitio es estático: necesita un backend externo (Formspree, Basin, o una
# función serverless propia). Sin esta variable, /contacto muestra el mail y
# LinkedIn en lugar del formulario, para no tragarse mensajes en silencio.
PUBLIC_FORM_ENDPOINT=
